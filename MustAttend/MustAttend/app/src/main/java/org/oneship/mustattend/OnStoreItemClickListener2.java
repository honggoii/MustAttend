package org.oneship.mustattend;

import android.view.View;

public interface OnStoreItemClickListener2 {

    // 아이템이 클릭되면 이 함수 호출
    public void onItemClick(RecommendAdapter.ViewHolder holder, View view, int position);
}

